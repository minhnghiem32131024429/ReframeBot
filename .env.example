# Copy this file to .env to avoid setting env vars every time
#
# Guardrail model path (prefer retrained model)
GUARDRAIL_PATH=D:\Work\AI\guardrail_model_retrained\best

# Use multi-turn context for guardrail classification
GUARDRAIL_CONTEXT_TURNS=3
GUARDRAIL_CONTEXT_MAX_CHARS=700

# Optional: semantic crisis detector thresholds
CRISIS_SEMANTIC_SIM_THRESHOLD=0.62
CRISIS_SEMANTIC_SIM_MARGIN=0.08

# Optional: embedding model for router + RAG
ROUTER_EMBED_MODEL=all-MiniLM-L6-v2
